{
	"devDependencies": {
		"@aws-sdk/client-dynamodb": "^3.922.0",
		"@aws-sdk/lib-dynamodb": "^3.922.0",
		"@types/lodash": "^4.17.13",
		"@types/node": "^22.10.2",
		"@vitest/coverage-v8": "^3.2.4",
		"eslint": "^9.26.0",
		"lodash": "^4.17.21",
		"prettier": "^3.4.2",
		"typescript": "^5.7.2",
		"typescript-eslint": "^8.32.0",
		"use-async-helpers": "^1.0.31",
		"vitest": "^3.2.4"
	},
	"peerDependencies": {
		"@aws-sdk/client-dynamodb": "^3.922.0",
		"@aws-sdk/lib-dynamodb": "^3.922.0",
		"lodash": "^4.17.21",
		"use-async-helpers": "^1.0.31"
	},
	"exports": {
		".": "./dist/index.js"
	},
	"files": [
		"dist"
	],
	"license": "MIT",
	"main": "dist/index.js",
	"name": "use-dynamodb",
	"repository": {
		"type": "git",
		"url": "https://github.com/feliperohdee/use-dynamodb"
	},
	"scripts": {
		"build": "rm -rf dist && yarn lint && tsc -p tsconfig.json",
		"dev:dynamodb": "java -jar /Users/feliperohde/Documents/dynamodb-local/DynamoDBLocal.jar -dbPath ./dynamodb-local -sharedDb",
		"lint": "prettier --write . && eslint --fix . && tsc -p tsconfig.json",
		"npm:publish": "yarn test && yarn build && yarn version --patch --no-git-tag-version && yarn publish --non-interactive",
		"test": "vitest --fileParallelism=false --run",
		"test:coverage": "rm -rf coverage && vitest --fileParallelism=false --run --coverage",
		"test:watch": "vitest --fileParallelism=false"
	},
	"version": "1.0.83"
}
